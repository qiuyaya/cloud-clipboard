{
  "name": "cloud-clipboard",
  "version": "1.0.12",
  "description": "A real-time cloud clipboard application with secure room-based sharing, desktop app support, automatic file management, and modern PWA features",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"bun run server:dev\" \"bun run client:dev\"",
    "build": "bun run shared:build && bun run icons:generate && bun run client:build && bun run build:production",
    "build:production": "bun run server:build && bun run copy-client",
    "copy-client": "rm -rf server/public && cp -r client/dist server/public",
    "icons:generate": "node scripts/generate-icons.js",
    "start": "bun run server:start",
    "server:dev": "cd server && bun run dev",
    "server:build": "cd server && bun run build",
    "server:start": "cd server && NODE_ENV=production bun run start",
    "client:dev": "cd client && bun run dev",
    "client:build": "cd client && bun run build",
    "shared:build": "cd shared && bun run build",
    "desktop:dev": "cd desktop && npm run dev",
    "desktop:build": "bun run icons:sync-desktop && cd desktop && node scripts/build-client.js && npm run build",
    "desktop:build-client": "cd desktop && node scripts/build-client.js",
    "icons:sync-desktop": "node scripts/sync-desktop-icons.js",
    "release": "node scripts/release.js",
    "release:patch": "node scripts/release.js patch",
    "release:minor": "node scripts/release.js minor",
    "release:major": "node scripts/release.js major",
    "release:dry-run": "node scripts/release.js patch --dry-run",
    "version:check": "node scripts/version-sync.js check",
    "version:report": "node scripts/version-sync.js report",
    "version:outdated": "node scripts/version-sync.js outdated",
    "test": "bun run shared:test && bun run server:test && bun run client:test",
    "test:watch": "concurrently \"bun run shared:test:watch\" \"bun run server:test:watch\" \"bun run client:test:watch\"",
    "test:coverage": "bun run shared:test:coverage && bun run server:test:coverage && bun run client:test:coverage && node scripts/coverage-report.js",
    "test:integration": "bun run server:test:integration",
    "test:e2e": "bun run client:test:e2e",
    "shared:test": "cd shared && bun test",
    "shared:test:watch": "cd shared && bun test --watch",
    "shared:test:coverage": "cd shared && bun test --coverage",
    "server:test": "cd server && bun test",
    "server:test:watch": "cd server && bun test --watch", 
    "server:test:coverage": "cd server && bun test --coverage",
    "server:test:integration": "cd server && bun test --grep integration",
    "client:test": "cd client && bun run test",
    "client:test:watch": "cd client && bun run test:watch",
    "client:test:coverage": "cd client && bun run test:coverage",
    "client:test:e2e": "cd client && bun run test:e2e",
    "lint": "eslint . --ext .ts,.tsx",
    "type-check": "cd shared && tsc --noEmit --skipLibCheck && cd ../server && tsc --noEmit --skipLibCheck",
    "shared:type-check": "cd shared && tsc --noEmit",
    "server:type-check": "cd server && tsc --noEmit",
    "client:type-check": "cd client && tsc --noEmit"
  },
  "workspaces": [
    "server",
    "client",
    "shared",
    "desktop"
  ],
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.39.1",
    "@typescript-eslint/parser": "^8.39.1",
    "@vitejs/plugin-react": "^5.0.0",
    "concurrently": "^8.0.0",
    "esbuild": "^0.25.9",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0",
    "vite": "^7.1.2",
    "@playwright/test": "^1.40.0",
    "supertest": "^6.3.0",
    "@types/supertest": "^6.0.0"
  }
}
